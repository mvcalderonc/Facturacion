import { TipoIdentificacion } from './tipo-identificacion';

describe('TipoIdentificacion', () => {
  it('should create an instance', () => {
    expect(new TipoIdentificacion()).toBeTruthy();
  });
});
